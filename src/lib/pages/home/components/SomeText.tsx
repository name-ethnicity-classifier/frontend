import { Grid, Heading, Text, Flex } from '@chakra-ui/react';

const SomeText = () => {
  return (
    <Flex textAlign="center" backgroundColor="red">
      <Text>rot</Text>
    </Flex>
  );
};

export default SomeText;
